import { ScrollParallaxDirective } from './scroll-parallax.directive';

describe('ScrollParallaxDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollParallaxDirective();
    expect(directive).toBeTruthy();
  });
});
