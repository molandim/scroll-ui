import { ScrollUiDirective } from './scroll-ui.directive';

describe('ScrollUiDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollUiDirective();
    expect(directive).toBeTruthy();
  });
});
