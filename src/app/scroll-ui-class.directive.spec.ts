import { ScrollUiClassDirective } from './scroll-ui-class.directive';

describe('ScrollUiClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollUiClassDirective();
    expect(directive).toBeTruthy();
  });
});
